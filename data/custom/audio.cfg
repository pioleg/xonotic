// Audio settings
echo "=> Executing Audio Configuration..."

// Volume
mastervolume "0.05"			// Master volume
bgmvolume "0"				// Music volume
snd_channel8volume "0"		// Music volume
snd_staticvolume "0"		// Ambient volume
snd_channel9volume "0"		// Ambient volume
snd_channel0volume "1"		// Info volume
snd_channel3volume "1"		// Items volume
snd_channel6volume "1"		// Pain volume
snd_channel7volume "1"		// Player volume (steps)
snd_channel4volume "1"		// Shots volume (projectiles)
snd_channel2volume "1"		// Voice volume (taunt)
snd_channel1volume "1"		// Weapons volume
snd_channel5volume "1"		// Weapons volume

// Other settings
snd_mutewhenidle "1"			// Mute when window is inactive
snd_attenuation_exponent "1"	// exponential sound falloff
snd_spatialization_control "1"	// Audio blending for headphones
cl_hitsound "1"					// Hitsound
con_chatsound "1"				// Chat souns
menu_sounds "0"					// Menu sounds
cl_announcer ""					// Announcer ("default" to turn on)
cl_announcer_maptime "3"		// Time announcer (0-off, 1-1 minute, 2-5 minutes, 3-both)
cl_autotaunt "0"				// Taunt chance (0-1)
